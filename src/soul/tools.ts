/**
 * Conway Automaton — Soul Tools
 * Soul tool implementations for the memory tool category.
 * (Core tool definitions are in agent/tools.ts)
 */

export { parseSoul, serializeSoul, soulHash } from './model.js';
export { validateSoul } from './validator.js';
export { performReflection, computeAlignment } from './reflection.js';
